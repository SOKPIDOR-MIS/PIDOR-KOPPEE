# Production configuration for Render
spring.application.name=coffee-shop-telegram-bot

# Telegram bot configuration (set these as environment variables in Render)
token=${TELEGRAM_BOT_TOKEN}
chat_id=${TELEGRAM_CHAT_ID}

# PostgreSQL database configuration for Render
spring.datasource.url=jdbc:postgresql://dpg-d5k7fa9b44c73a8f4qp-a.oregon-postgres.render.com:5432/sdb
spring.datasource.username=sdb_user
spring.datasource.password=D5JRKbz9f4400iZhQLk6VN0DT52eoz7u

# PostgreSQL dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.id.new_generator_mappings=false
spring.jpa.properties.hibernate.format_sql=true

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=true

# Database connection pool settings
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.auto-commit=true

# Server configuration
server.error.include-message=always

# Actuator for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always