spring.application.name=coffee-shop-telegram-bot

# Telegram bot configuration with environment variables
token=${TELEGRAM_BOT_TOKEN:8362519680:AAEiM_2ZnjkAcsrtmyYDdzM-DfdtmdTqXjA}
chat_id=${TELEGRAM_CHAT_ID:773705790}

# Database configuration with environment variables

spring.datasource.url=jdbc:mysql://localhost:3306/dbsu6?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=

# PostgreSQL dialect
spring.jpa.properties.hibernate.dia	lect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=true

# Database connection pool settings
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.auto-commit=true

# Server configuration
server.error.include-message=always

# Actuator for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always